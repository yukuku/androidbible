<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- This legacy wizard has no inset handling of its own, so opt out of
         the Android 15 edge-to-edge enforcement like the main app theme does
         for its not-yet-migrated windows. -->
    <style name="AlkitabFeedback.Theme" parent="AlkitabFeedback.Theme.Base">
        <item name="android:windowOptOutEdgeToEdgeEnforcement">true</item>
    </style>
</resources>
